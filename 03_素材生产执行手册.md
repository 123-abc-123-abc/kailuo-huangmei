# 《开锣》素材生产执行手册

> 提示词本体见 `04_美术资源规范与生图提示词.md`（v3.2 现行部分在文末"v3.2 老槐树版"章）。本手册回答：按什么顺序生产、怎么算合格、怎么留档、工具怎么用。生成日志模板在文末。
>
> **执行状态（9/5 核销）**：D2-D3 排期已全部完成——图片 21/21 生成并接入（v2 批），v3.2 场景 5 张重制接入；音频引擎 v3 完成（外挂 BGM+锣鼓录音已接）；视频 v1 完成待重生成；LLM 已接智谱 GLM-5.3-Flash。**剩余生产项**：v3.2 结局图 3 张、L6 七仙女/L7 董永立绘（提示词见 04 文档 v3.2 章）、满工真唱段（用户提供海绵音乐产出后一行接入）。详细执行记录见 `prototype/assets/log/素材生成日志.md`。

***

## 一、生产排期（D2-D3，9/3-9/4，已全部完成，留档）

| 批次    | 内容                                 | 耗时预估 | 说明                   |
| ----- | ---------------------------------- | ---- | -------------------- |
| 第 1 批 | 小满三视图 + 云秋/陈团长/韩师傅立绘（P0 共 5 张）     | 半天   | **一致性锚点先行**，全部后续素材依赖 |
| 第 2 批 | 场景 5 张（无人物，不依赖锚点，可与第 1 批并行）        | 2 小时 | 每场景生成 4-8 张选 1       |
| 第 3 批 | 物件特写 8 张                           | 2 小时 | 不依赖角色锚点              |
| 第 4 批 | P1 立绘（小满戏装/云秋戏装/老魏）+ 老照片 + 关键帧 3 张 | 半天   | 依赖第 1 批锚点（参考图功能）     |
| 第 5 批 | 音频测试（锣鼓/唱段/主题曲）                    | 半天   | **先测戏腔效果再决定唱段方案**    |
| 第 6 批 | 视频 1-2 段（关键帧图生视频）                  | 半天   | 放在素材全部达标后            |

## 二、验收标准

| 类别    | 标准                                          |
| ----- | ------------------------------------------- |
| 立绘    | 写实国风不跑偏（忌幼态/二次元）；人物特征与角色卡锚点一致；手部收袖/背手零风险姿态；纵向构图 |
| 场景    | 无人物（或仅剪影）；构图留白；与相邻场景色调统一（后台系暖黄、练功系晨青、舞台系红金） |
| 一致性抽检 | 同一角色不同图放一起，3 秒内能认出是同一人（达不到就加参考图或改用 LoRA）    |
| 整体    | 通过率 = 可用张数/生成总数，逐批记录（报告"生成流程"用）             |
| 音频    | 唱段戏腔听感成立（不跑调、吐字可辨）；不成立立即执行降级预案（念白+锣鼓）       |
| 视频    | 15-30s；首帧尾帧与关键帧一致；无明显肢体崩坏                   |

## 三、工具操作要点

### 即梦（生图主力）

1. **参考图/智能参考**：上传角色锚点图 → 输入新场景描述（如"同一人物，侧台对戏"）→ 保持角色一致。每次同时上传 3-5 张参考图效果更稳。（现行待生成角色：L6 七仙女 / L7 董永，首图即锚点）
2. 免费积分每日发放、当日清零 → **每天额度用完即止，未达标素材顺延次日**，不要熬夜堆砌重试。
3. 风格锁定以 `04_美术资源规范与生图提示词.md` 为准（写实国风，全局正反向基底+v2 提示词），旧水墨后缀已废止。

### Midjourney（备选）

角色一致性用 `--cref 三视图URL --cw 80`（80 折中：保脸部与大体服装，允许换装）。

### 哩布 LoRA（一致性兜底）

若参考图法仍不达标：用该角色定妆图 15-20 张训练专属 LoRA（每月有免费次数），后续生图挂载。

### 网易天音/海绵音乐（唱段）

- 测试提示词方向："黄梅戏戏腔，女声，江南小调，五声音阶，曲目主题：离别与团圆"

- 每工具至少测 3 首；同时测试"念白+锣鼓"降级方案的可听性

- **当天记录结论：走唱段 or 走念白**，写入生成日志，此决策影响第六幕演出呈现

### 可灵/即梦（视频）

- 图生视频：先出关键帧图（第 4 批），再"图生视频+运镜描述（镜头缓推）"

- 单段生成多出几条备选；选片标准=一致性>美感

### 豆包/讯飞 TTS（彩蛋戏腔）

- 彩蛋生成的唱词用 TTS 朗读时选"戏曲/古风"音色；仅 P2，时间不够直接文字展示

## 四、素材接入原型的方法

1. 素材按 02 文件的命名规范放入 `prototype/assets/` 对应子目录；
2. 打开 `prototype/index.html`，搜索 `ASSET:` 注释定位接入点；
3. 修改 `CFG.assets`：把对应项从 `null` 改为 `'assets/char/xiaoman_suzhuang.png'`；
4. 浏览器本地打开 `index.html` 验证显示；无图时原型以占位色块+文字运行，**素材接入前游戏完整可玩**。

## 五、LLM 接入清单（D4，已完成——实际接入智谱，9/5）

| 接入点             | 功能                  | 实际模型            | 备注                         |
| --------------- | ------------------- | --------------- | -------------------------- |
| `llmChat()`     | 每折 NPC 实时对话（persona 制：七仙女/董永/云秋） | 智谱 GLM-5.3-Flash | OpenAI 兼容端点；`reasoning_effort:"low"`（max 档思考 26s 不可用）；密钥走 secrets.local.js 不落主文件；含模板兜底 |
| `generateEgg()` | 尾声编新词彩蛋             | 同上              | 提示词要求：黄梅调七字句、合辙押韵、嵌用户词、纯中文 |
| 降级方案            | API 不可用/密钥缺失时            | 本地模板库           | 对话每人 5 条 mock；彩蛋模板 v3.3 起扩至 15 组 |

> 9/5 整改新增：对话历史（每 persona 最近 4-6 条 messages）、回复上限 90 字、输出端敏感词过滤、AI 旁白引用（自由对话在叙事中留痕）、路遇折七仙女/董永双对象切换。详见 `05_双视角测评报告.md` 与素材日志。

## 六、生成日志模板（assets/log/ 每张素材一行，Markdown 表格追加）

```markdown
| 日期 | 素材名 | 工具 | 提示词全文 | 重试次数 | 淘汰原因摘要 | 终选理由 |
|------|--------|------|-----------|---------|--------------|---------|
| 9/3 | char_xiaoman_suzhuang | 即梦 | （全文粘贴） | 6 | 手部错误×2、风格跑偏×3 | 第7张风格与特征完全符合锚点 |
```

留档三重价值：①报告"生成流程"章节的数据支撑；②"人类智力投入"的著作权证据链（呼应北京互联网法院 AI 文生图案裁判要点）；③复盘改进依据。

## 七、每日收工检查（D2-D3 执行）

- [ ] 今日素材已入 `assets/` 并更名合规

- [ ] 生成日志已补全（含淘汰记录）

- [ ] 通过率统计已更新

- [ ] 未达标项已列入明日批次

- [ ] 明日额度用尽的工具已标记顺延

